<ui:FluentWindow
    x:Class="NotToday.Wins.ColorPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NotToday.Wins"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="ColorPickerWindow"
    Width="500"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="选择目标颜色"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/logo.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.Resources>
                    <Style TargetType="Button" />
                    <Style TargetType="ComboBox" />
                    <Style TargetType="ComboBoxItem" />
                    <Style TargetType="TextBox" />
                    <Style TargetType="TabControl" />
                    <Style TargetType="TabItem" />
                    <Style TargetType="TextBlock" />
                </Grid.Resources>
                <!--<colorpicker:StandardColorPicker x:Name="ColorPicker" />-->
            </Grid>
            <StackPanel
                Grid.Row="1"
                Margin="0,8"
                Orientation="Horizontal">
                <Button Click="Confirm_Click" Content="确定" />
                <Button Click="Cancel_Click" Content="取消" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
