<ui:FluentWindow
    x:Class="NotToday.Wins.SelecteCaptureAreaWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:imagecropper="clr-namespace:NotToday.Controls.ImageCropper"
    xmlns:local="clr-namespace:NotToday.Wins"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SelecteCaptureAreaWindow"
    Width="1000"
    Height="600"
    ExtendsContentIntoTitleBar="True"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="选择监测区域"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/logo.ico" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid x:Name="DisplayArea">
                <imagecropper:ImageCropper
                    x:Name="ScreenshotImage"
                    CropShape="Rectangular"
                    CropperEnable="True"
                    DragImgEnable="True"
                    MinCroppedPixelLength="1"
                    ThumbPlacement="All" />
            </Grid>
            <Border
                x:Name="OperationArea"
                Grid.Row="1"
                Padding="0,8">
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <Button
                        Margin="8,0"
                        Click="UpdateScreenshot_Click"
                        Content="刷新截图" />
                    <Button
                        Margin="8,0"
                        Click="Confirm_Click"
                        Content="保存区域" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
